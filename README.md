# Telegram bot: slot triple -> Stars gift

Бот работает в группах и отслеживает сообщения-слоты `🎰`.
Виграш рахується, якщо:
- випали будь-які `3` однакові символи (4 варіанти), або
- випали будь-які `2` сімки (включно з `777`).

Шанс виграшу за один спін: `13/64 = 20.3125%`.

## Логика подарков

- Ліміт призів за день на групу задається параметром `DAILY_PRIZE_LIMIT` (типово `1`).
- Якщо денний ліміт уже вичерпано, бот відповідає на кожну спробу крутити `🎰`, що ліміт на сьогодні закрито.
- Автопоповнення Stars доступне через бізнес-акаунт і команди в особистому чаті (`/topup`, `/buy`).
- У повідомленні про виграш бот показує, хто саме виграв і з якої спроби за поточний день.
- Для звичайних призів використовується фіксований random за вагами:
  - `50 Stars` -> `3/31` (`9.68%`)
  - `25 Stars` -> `6/31` (`19.35%`)
  - `15 Stars` -> `22/31` (`70.97%`)
- Внутри выбранной категории бот выбирает случайный реальный подарок из `getAvailableGifts`.
- Если для выбранной цены нет доступных подарков, бот пытается выдать ниже по цене (`50 -> 25 -> 15`, `25 -> 15`).
- Місячне обмеження залишено лише для NFT-пулу (`MONTHLY_NFT_POOL`).
- 14 лютого (Valentine): при виграші бот намагається видати подарунок рівно на `50 Stars` з пріоритетного списку `VALENTINE_50_GIFT_IDS` (2 gift_id).

Состояние хранится в SQLite (`gift_state.sqlite3` по умолчанию), поэтому ограничения сохраняются между рестартами.

## Запуск

1. Установить Python 3.10+.
2. Создать и активировать виртуальное окружение.
3. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Создать `.env`:
   ```bash
   cp .env.example .env
   ```
5. Запустить:
   ```bash
   python bot.py
   ```

## Переменные окружения

- `BOT_TOKEN` - токен от `@BotFather`.
- `DB_PATH` - путь к SQLite-файлу (по умолчанию `gift_state.sqlite3`).
- `TZ` - часовой пояс для дневного/месячного лимита (по умолчанию `Europe/Kyiv`).
- `DAILY_PRIZE_LIMIT` - скільки призів на день можна видати в одній групі (типово `1`, діапазон `0..100`).
- `ALLOWED_CHAT_IDS` - whitelist груп у форматі CSV (наприклад: `-100123,-100456`).
- `AUTO_TOPUP_ENABLED` - увімкнути автопоповнення (`1`/`0`, за замовчуванням `1`).
- `AUTO_TOPUP_THRESHOLD` - поріг Stars у боті, нижче якого запускається поповнення (за замовчуванням `100`).
- `AUTO_TOPUP_AMOUNT` - скільки Stars переказувати за один раз (за замовчуванням `615`).
- `STATS_OWNER_USER_ID` - Telegram user id власника, якому доступна команда `/stats` у приватному чаті.
- `BUSINESS_CONNECTION_ID` - можна вказати вручну; якщо не вказано, бот спробує зберегти його автоматично з бізнес-апдейтів.
- `VALENTINE_50_GIFT_IDS` - CSV список `gift_id` для 14 лютого (пріоритетні 50-star подарунки).

Безпека:
- Бот працює тільки в групах із `ALLOWED_CHAT_IDS`.
- Якщо `ALLOWED_CHAT_IDS` порожній, бот не обробляє жоден чат.
- Для тесту слотів у приватному чаті бот відповідає лише власнику (`STATS_OWNER_USER_ID`).

## Настройка в Telegram

1. Создай бота через `@BotFather` и получи `BOT_TOKEN`.
2. В `@BotFather` отключи privacy mode:
   `/setprivacy` -> выбери бота -> `Disable`.
3. Добавь бота в группу и дай право отправлять сообщения.
4. Для выдачи Stars gift у бота должен быть доступ/баланс для отправки подарков.

## Business Bot

Можно привязать этого бота к бизнес-аккаунту в Telegram:

1. `Settings -> Telegram Business -> Chatbots`.
2. Подключи бота.
3. Включи разрешения на работу в группах (если доступно в клиенте).

## Railway

Можна деплоїти на Railway як `Worker` (без HTTP-порту), стартова команда вже готова: `python bot.py`.

1. Підключи репозиторій у Railway.
2. Додай Environment Variables з `.env.example` (мінімум `BOT_TOKEN`).
3. Задеплой сервіс.

Важливо: Railway-файлова система може бути ефермерною. Якщо лишиш `SQLite` у файлі (`gift_state.sqlite3`) без persistent volume, ліміти/стан можуть скидатися після redeploy/restart.

## Команда /stats

- Команда `/stats` працює лише в особистому чаті з ботом.
- Якщо задано `STATS_OWNER_USER_ID`, команду `/stats` бачить лише цей користувач.
- Показує баланс Stars бота, параметри автопоповнення, `business_connection_id`, денний ліміт та статус NFT-пулу у whitelisted групах.

## Команда /dailylimit

- Команда `/dailylimit` працює лише в особистому чаті з ботом для `STATS_OWNER_USER_ID`.
- `/dailylimit` без аргументів показує поточний ліміт і скільки призів уже видано сьогодні в кожній групі з `ALLOWED_CHAT_IDS`.
- `/dailylimit 2` встановлює новий ліміт призів на день (діапазон `0..100`).
- `0` означає повністю вимкнути видачу денних призів.
- Команда також показує оцінку витрат Stars за місяць (без урахування NFT-призів).

## Команда /ids

- Команда `/ids` працює в будь-якому чаті.
- Повертає `chat_id` поточного чату і `user_id` користувача, який викликав команду.
- Зручно для зв'язки `bot + payer`:
  - payer пише `/ids` у спільному чаті;
  - бот показує `chat_id` (для бота) і `user_id` payer-а.

## Команда /topup

- Команда `/topup` працює лише в особистому чаті з ботом.
- Якщо задано `STATS_OWNER_USER_ID`, команду може виконати лише цей користувач.
- `/topup` без аргументу поповнює на `AUTO_TOPUP_AMOUNT`.
- `/topup 615` поповнює на вказану суму (діапазон `1..10000`).

## Команда /buy

- Команда `/buy` працює лише в особистому чаті з ботом.
- Якщо задано `STATS_OWNER_USER_ID`, команду може виконати лише цей користувач.
- `/buy 615` надсилає інвойс на оплату в Stars (`XTR`), щоб поповнити баланс бота через платіж.

## Команда /gifts

- Команда `/gifts` працює лише в особистому чаті з ботом.
- Показує список доступних подарунків: `id`, `stars`, `convert`, `sellable`, `sold_out`.
- Допомагає перевірити, які подарунки бот вважає "продаваемыми".

## Команда /nfts

- Команда `/nfts` працює лише в особистому чаті з ботом.
- Якщо задано `STATS_OWNER_USER_ID`, команду може виконати лише цей користувач.
- Показує список `unique gifts` бізнес-акаунта: `owned_gift_id`, `gift_id`, `model/symbol`, можливість трансферу та fee.

## Команда /transfernft

- Команда `/transfernft` працює лише в особистому чаті з ботом.
- Якщо задано `STATS_OWNER_USER_ID`, команду може виконати лише цей користувач.
- Формат:
  - `/transfernft <owned_gift_id> <user_id>`
  - `/transfernft <owned_gift_id> <user_id> <star_count>`
- Команда викликає `transferGift` і передає `unique gift` користувачу.

## Команда /teststicker

- Команда `/teststicker` працює лише в особистому чаті з ботом для `STATS_OWNER_USER_ID`.
- Дозволяє протестувати відправку візуального подарунка без виграшу.
- Команда без параметрів: бот вибирає випадковий подарунок із діапазону `15/25/50 Stars`.

## Команда /resetday

- Команда `/resetday` працює лише в особистому чаті з ботом для `STATS_OWNER_USER_ID`.
- Скидає прапорець "сьогодні вже є переможець" для групи, щоб можна було знову крутити.
- Формати:
  - `/resetday -1001234567890`
  - `/resetday -1001234567890 2026-02-10`
- При скиданні бот також відкочує місячні лічильники за всі призи, видані в цей день.
